package com.iver.controller.service.provider;

import com.iver.controller.model.DevicePackage;

public interface DevicePackageProvider {
    DevicePackage saveDevicePackage(DevicePackage newDevicePackage);
}